---
services:
  - docker

env:
#-----------------------------------------------
#
# CentOS
#
#-----------------------------------------------
  - distribution: centos
    version: "7"
    playbook: packer-latest
  - distribution: centos
    version: "7"
    playbook: packer-1.2.5
  - distribution: centos
    version: "6"
    playbook: packer-latest
  - distribution: centos
    version: "6"
    playbook: packer-1.2.5

#--------------------------------
#
# Fedora
#
#--------------------------------
  - distribution: fedora
    version: "27"
    playbook: packer-latest
  - distribution: fedora
    version: "27"
    playbook: packer-1.2.5
  - distribution: fedora
    version: "26"
    playbook: packer-latest
  - distribution: fedora
    version: "26"
    playbook: packer-1.2.5

#--------------------------------
#
# Ubuntu
#
#--------------------------------
  - distribution: ubuntu
    version: "19.04"
    playbook: packer-latest
  - distribution: ubuntu
    version: "19.04"
    playbook: packer-1.2.5
  - distribution: ubuntu
    version: "18.04"
    playbook: packer-latest
  - distribution: ubuntu
    version: "18.04"
    playbook: packer-1.2.5
  - distribution: ubuntu
    version: "16.04"
    playbook: packer-latest
  - distribution: ubuntu
    version: "16.04"
    playbook: packer-1.2.5
  - distribution: ubuntu
    version: "14.04"
    playbook: packer-latest
  - distribution: ubuntu
    version: "14.04"
    playbook: packer-1.2.5
  - distribution: ubuntu
    version: "12.04"
    playbook: Packer-latest
  - distribution: ubuntu
    version: "12.04"
    playbook: Packer-1.2.5

#--------------------------------
#
# Debian
#
#--------------------------------
  - distribution: debian
    version: "9"
    playbook: packer-latest
  - distribution: debian
    version: "9"
    playbook: packer-1.2.5
  - distribution: debian
    version: "8"
    playbook: packer-latest
  - distribution: debian
    version: "8"
    playbook: packer-1.2.5

#--------------------------------
#
# Oraclelinux
#
#--------------------------------
  - distribution: oraclelinux
    version: "7"
    playbook: packer-latest
  - distribution: oraclelinux
    version: "7"
    playbook: packer-1.2.5
  - distribution: oraclelinux
    version: "6"
    playbook: packer-latest
  - distribution: oraclelinux
    version: "6"
    playbook: packer-1.2.5

#--------------------------------
#
# Amazonlinux
#
#--------------------------------
  - distribution: amazonlinux
    version: "2"
    playbook: packer-latest
  - distribution: amazonlinux
    version: "2"
    playbook: packer-1.2.5
  - distribution: amazonlinux
    version: "1"
    playbook: packer-latest
  - distribution: amazonlinux
    version: "1"
    playbook: packer-1.2.5

#--------------------------------
#
# Opensuse
#
#--------------------------------
  - distribution: opensuse
    version: "15.1"
    playbook: packer-latest
  - distribution: opensuse
    version: "15.1"
    playbook: packer-1.2.5
  - distribution: opensuse
    version: "15"
    playbook: packer-latest
  - distribution: opensuse
    version: "15"
    playbook: packer-1.2.5
  - distribution: opensuse
    version: "42.3"
    playbook: packer-latest
  - distribution: opensuse
    version: "42.3"
    playbook: packer-1.2.5
  - distribution: opensuse
    version: "42.2"
    playbook: packer-latest
  - distribution: opensuse
    version: "42.2"
    playbook: packer-1.2.5
  - distribution: opensuse
    version: "13.2"
    playbook: packer-latest
  - distribution: opensuse
    version: "13.2"
    playbook: packer-1.2.5

#--------------------------------
#
# Archlinux
#
#--------------------------------
  - distribution: archlinux
    version: "rolling"
    playbook: packer-latest
  - distribution: archlinux
    version: "rolling"
    playbook: packer-1.2.5

#--------------------------------
#
# Alpine
#
#--------------------------------
  - distribution: alpine
    version: "rolling"
    playbook: packer-latest
  - distribution: alpine
    version: "rolling"
    playbook: packer-1.2.5

before_install:
  # Build docker container
  - 'sudo docker pull diodonfrost/${distribution}-${version}-ansible:latest'

script:
  - container_id=$(mktemp)

  # Run container and mount ansible role with it
  - 'sudo docker run --detach --privileged -v /sys/fs/cgroup:/sys/fs/cgroup:ro --volume="${PWD}":/etc/ansible/roles/ansible-role-Packer:ro diodonfrost/${distribution}-${version}-ansible:latest > "${container_id}"'

  # Check syntax
  - 'sudo docker exec "$(cat ${container_id})" env ANSIBLE_FORCE_COLOR=1 ansible-playbook -v /etc/ansible/roles/ansible-role-Packer/tests/playbooks/${playbook}.yml --syntax-check'

  # Run ansible playbook
  - 'sudo docker exec "$(cat ${container_id})" env ANSIBLE_FORCE_COLOR=1 ansible-playbook -v /etc/ansible/roles/ansible-role-Packer/tests/playbooks/${playbook}.yml'

  # Test indempotence
  - >
    sudo docker exec "$(cat ${container_id})" env ANSIBLE_FORCE_COLOR=1 ansible-playbook -v /etc/ansible/roles/ansible-role-Packer/tests/playbooks/${playbook}.yml
    | grep -q 'changed=0.*failed=0'
    && (echo 'Idempotence test: pass' && exit 0)
    || (echo 'Idempotence test: fail' && exit 1)

  # Test role
  - 'inspec exec tests/inspec -t docker://$(cat ${container_id})'

  # Delete container
  - 'sudo docker rm -f "$(cat ${container_id})"'

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
