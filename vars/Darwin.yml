---
# vars file for packer in Mac osx system
_packer_pkg_url: "https://releases.hashicorp.com/packer/{{ _packer_version }}/packer_{{ _packer_version }}_darwin_{{ _base_arch[ansible_facts['architecture']] | default('arm64') }}.zip"
_packer_path: /usr/local/bin/
